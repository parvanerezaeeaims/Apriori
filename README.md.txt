# Apriori

This project analyzes customer grocery transactions using the Apriori algorithm. The results are visualized using Streamlit, providing insights into frequently bought itemsets and their associations.

## Table of Contents
- [Installation]
- [Usage]
- [Project Structure]
- [Dependencies]


## Installation

1. **Clone the repository:**
    ```bash
    git clone https://github.com/yourusername/your-repo-name.git
    cd your-repo-name
    ```

2. **Install the required dependencies:**
    ```bash
    pip install -r requirements.txt
    ```
3. **Ensure your data file (`Apriori.xlsx`) is placed in the correct directory:**
    - Place `Apriori.xlsx` in the `apriori` directory.

## Usage

1. **Navigate to the project directory:**
    ```bash
    cd /path/to/your/project
    ```

2. **Run the Streamlit app:**
    ```bash
    streamlit run app.py
    ```

3. **Open your web browser and go to the provided URL to interact with the app.**

## Project Structure

Apriori/
│
├── .streamlit/
│ ├── config.toml 
│
├── IPython Notebook/
│ ├── Apriori.ipynb
│
│
│
├── app.py/
├── Apriori.py/
├── Apriori.xlsx/
│
│
├── Dashboard_Sample.png
├── requirements.txt 
└── README.md 



## Dependencies

The project requires the following Python packages:

- streamlit
- pandas
- apyori
- tabulate

You can install all dependencies using:

```bash
pip install -r requirements.txt



